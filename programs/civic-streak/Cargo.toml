[package]
name = "civic-streak"
version = "0.1.0"
description = "A Solana program for tracking civic engagement streaks"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "civic_streak"

[dependencies]
anchor-lang = { version = "0.32.1", features = ["idl-build"] }
